# Plan

Build paper 109 `non_smooth_mechanics_for_foundation_policies` from the shared pool, compile PDF to Downloads only, and publish the exact-name public repo.

Continuation rule: the v4.1 audit must remain evidence-bound. Keep `STRONG_REVISE` only if the rerun reproduces the contact-mode advantage, safety/cost non-regression, paired-seed wins, ablation margin, stress-sweep ordering, and artifact-location gates. Do not mark ICLR-main-ready without real robot or independent high-fidelity validation.
